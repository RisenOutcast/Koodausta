using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace RO.Crab
{
    public class Saksi : MonoBehaviour
    {
        // Update is called once per frame
        void Update()
        {
            Destroy(gameObject, 0.34f);
        }
    }
}